name: Premarket Quant Report

on:
  schedule:
    # Fallback when the Cloudflare Worker scheduler is down; delivery is restricted to Beijing 08:32-08:42.
    - cron: "29 0 * * 1-5"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  report:
    uses: ./.github/workflows/daily-report.yml
    with:
      session: premarket
      scheduled_run: ${{ github.event_name == 'schedule' }}
      delivery_target: "08:37"
      delivery_tolerance_minutes: 5
      silent_failure: ${{ github.event_name == 'schedule' }}
    secrets: inherit
